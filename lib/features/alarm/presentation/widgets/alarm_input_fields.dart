import 'package:flutter/material.dart';

class AlarmInputFields extends StatelessWidget {
  const AlarmInputFields({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
