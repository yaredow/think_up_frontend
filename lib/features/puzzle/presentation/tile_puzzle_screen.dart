import 'package:flutter/material.dart';

class TilePuzzleScreen extends StatelessWidget {
  const TilePuzzleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
