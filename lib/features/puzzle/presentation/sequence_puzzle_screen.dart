import 'package:flutter/material.dart';

class SequencePuzzleScreen extends StatelessWidget {
  const SequencePuzzleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
